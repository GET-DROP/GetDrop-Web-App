
{{#if success_msg }}
  <div class="chip"> {{success_msg}}
        <i class="close material-icons">close</i>
        </div>{{else}}
        {{/if}}

{{#if error_msg}}
  <div class="chip"> {{error_msg}}
        <i class="close material-icons">close</i>
        </div>{{else}}
        {{/if}}

      {{#if error}}
  <div class="chip"> {{error}}
        <i class="close material-icons">close</i>
        </div>{{else}}
        {{/if}}
